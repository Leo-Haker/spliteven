# Copy this file to .env and replace the placeholders with your local values.
# The root .env file is ignored by Git and must never be committed.

# Gmail address used as the sender for membership invitations.
MAIL_USERNAME=your-email@example.com

# Gmail app password for SMTP authentication. Do not use your normal password.
MAIL_PASSWORD=your-gmail-app-password
